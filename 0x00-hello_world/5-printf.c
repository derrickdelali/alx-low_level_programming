#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: A C Program that prints with a printf function.
 *
 * Return: Always return with a 0 value
 */
int main(void)
{
printf(stder, "with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
